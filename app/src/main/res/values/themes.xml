<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PPKNews" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/white</item>
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:actionBarWidgetTheme">@style/Theme.PPKNews.Widget</item>
    </style>

    <style	name="Theme.PPKNews.AppBarOverlay"	parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <item name="colorControlNormal">@color/white</item>
    </style>
    <style	name="Theme.PPKNews.PopupOverlay"	parent="ThemeOverlay.AppCompat.DayNight">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="Theme.PPKNews.Widget" parent="Theme.MaterialComponents.DayNight">
        <item name="android:textColorHint">@android:color/white</item>
        <!-- your other custom widget styles -->
    </style>
</resources>